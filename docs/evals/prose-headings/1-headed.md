Yes — `~/.bashrc:257-266` defines `claude_wd`, which runs `claude --append-system-prompt`, and lines 269-273 alias it as `clddocs`, `cldydocs`, and the model variants. It appends the docs-repo diagram context, not the Katharsis rules: the comment at lines 245-246 says writing.md is deliberately excluded because your AGENTS.md import already loads the rules into every session, so appending would load them twice. That matches the earlier tracker finding that no Katharsis append-mode alias exists yet — the kclaude alias from that tracker item is still unbuilt.

Both are already in place, so there is nothing to add.

`work-request.md:58` carries `## Risks` as slot 3 of the Shape, between Findings and Decisions, with the conditional-versus-true-now split stated. And the broader point you're making is now the global rule: the output style's Reference codes section opens with "Every code below applies in every exchange type," and each file's Reference codes line says the list is "what is common here, never a requirement or a limit."

On how a risk shows up in a factual question: as a clause on the answer, not as a coded line. `factual-question.md` slot 3 is "a trailing clause naming a risk, when the answer is true but fragile. Attach it to the answer rather than promoting it to a group." The worked example is the conditional one at the bottom of that file — "They do not conflict today. Both match the same cue, so a third rule on either side makes the match order decide the outcome." The second sentence is a risk by the definition in the table: not yet gone wrong, and it changes what you do if it does.

The reason it stays uncoded there is what the code is for. A code earns its place by saving you sorting, and a 24-word answer has nothing to sort — a `## Risks` header over one clause adds a structure you have to read past to reach the sentence. The code appears in that type only when the answer is long enough that the risk would otherwise get lost, which the "never a limit" line already permits.

## Questions

❓ **Q9** - **Should `factual-question.md`'s Reference codes line name `R` alongside `C` and `Q`?** - it currently lists only those two as common, and `R` reaches the type through the general rule rather than by name.

   a. leave it — the clause form in slot 3 is the intended shape, and naming `R` invites the coded form in the type least able to afford it
   b. add `R` to the "sometimes" list, with a pointer to slot 3 saying the clause is preferred until the answer runs long

➡️ **b** - the trade-off is invitation against discoverability, and a reader who checks only the Reference codes line currently sees no risk handling at all in this type; the pointer keeps the clause as the default while making the escape hatch findable.
